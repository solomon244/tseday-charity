# Proxied to backend in dev (see next.config.mjs rewrites)
BACKEND_URL=http://localhost:4000

# Must match backend ADMIN_SECRET (for /admin page session check)
ADMIN_SECRET=

# Optional: call API directly when not using same-origin rewrites
# NEXT_PUBLIC_API_URL=http://localhost:4000

NEXT_PUBLIC_SITE_URL=http://localhost:3000
